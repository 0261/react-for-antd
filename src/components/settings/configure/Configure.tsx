import React from 'react';
import styles from './Configure.scss';

const Configure: React.FunctionComponent = children => {
    return <div className={styles.configure}>This is Configure</div>;
};
export default Configure;
